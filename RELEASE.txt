v04.03.00:
- Updated `frontend/app.js` to render dynamic board states and respond to human clicks.
- Added live Chat message rendering logic using WebSocket events.
- Handled Game Over UI state, disabling inputs upon completion.

v04.02.00:
- Added `frontend/app.js` with HTTP Lobby Integration (`/match`, `/join`).
- Established native WebSocket connection with Event Router pattern.
- Bound UI buttons for Match creation and connection toggling.

v04.01.00:
- Extracted UI into a clean `frontend/index.html` structure.
- Implemented Glassmorphism CSS system in `frontend/styles.css`.
- Added CSS Grid 3x3 layout for the board and Flexbox layout for the Chat Area.

v03.03.00:
- Extracted LLM Structured Output using Pydantic and Google Gen AI API schemas.
- Implemented move validation and retry loop to prevent AI hallucination.
- Wired AI output to WebSocket submission.

v03.02.00:
- Added `google-genai` SDK and created `GeminiClient` implementing `LLMClient` interface.
- Added `MemoryWindow` to `client/agent.py` to buffer recent WebSocket events.
- Added dynamic prompt building combining Persona, Memory, Board State, and Valid Moves.
- Integrated `GeminiClient` to generate moves when it is the Agent's turn.

v03.01.00:
- Added `client/agent.py` CLI script.
- Added Lobby HTTP integration for Auth Tokens.
- Added WebSocket connection loop and event parsing.

v02.03.00:
- Added `ActiveMatch` to WebSocket `ConnectionManager`.
- Wired `TicTacToe` into WebSocket loop.
- Added E2E tests for Match Lifecycle in `test_match_lifecycle.py`.

v02.02.00:
- Built Tic-Tac-Toe Engine implementing GameInterface.
- Wrote rigorous tests covering winning permutations and out-of-bounds states.

v02.01.00:
- Added `games/interface.py` with `GameInterface`.
- Configured MyPy for strict typing in `games/`.

v01.03.00:
- Added WebSocket Schemas and ConnectionManager.
- Implemented `/ws/match/{match_id}` endpoint with Auth Handshake.
- Added Event Routing and Broadcast Loop.
- Added Concurrent WebSocket Tests.

v01.02.00:
- Added Async Database Engine (aiosqlite).
- Defined Match, MoveLog, and ChatLog schemas.
- Implemented Repository Pattern for DB.
- Added database verification script and foreign key constraint tests.

v01.01.00:
- Added FastAPI skeleton and health probe.
- Added lobby match generation endpoint.
- Added lobby join and auth token generation endpoint.
