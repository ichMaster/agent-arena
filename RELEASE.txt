AgentArena — Release Notes
==========================

Version 05.01.00 (2026-07-21)
------------------------------
Version v05.01 — Resilience & reconnection. First version of phase v05.
Git tag: opus-opus-v05.01.00 (namespaced — this branch's independent build).

- server/main.py: the WS receive loop no longer crashes on malformed
  input -- invalid JSON, valid-but-non-object JSON, and a raw binary
  frame all yield a graceful error event and keep the connection open and
  usable, closing a real gap found during reconciliation (this version's
  own DoD was not actually implemented before this release).
  (ARENA-OPUS-OPUS-034)
- tests/test_reconnect.py: a same-token reconnect reclaims its seat after
  a drop; the two-seat invariant holds with no leak. (ARENA-OPUS-OPUS-034)
- Confirmed already shipped (v01.04/v02.03): seat release + shielded
  finally cleanup, dead-socket broadcast pruning, bad-token 4001 close,
  model-error legal fallback.
- Review fix: a binary WS frame no longer crashes the connection either
  (code review #1, MEDIUM).
- Quality: 181 tests passing; mypy --strict clean; zero paid calls.

Version 04.02.00 (2026-07-21)
------------------------------
Version v04.02 — Arena orchestration script. **v04 release gate**: phase
v04 (Agent-vs-Agent Orchestration) is complete.
Git tag: opus-opus-v04.02.00 (namespaced — this branch's independent build).

- scripts/run_arena.sh: one command creates a match, launches Ironclaw (X)
  then waits for its join before launching Bastion (O) -- ordering, not a
  flag, makes seats deterministic. Prints the full match id + Observe-not-
  Join instructions; traps INT/TERM/EXIT to clean up; tails both logs.
  (ARENA-OPUS-OPUS-033)
- Review: no findings.
- Quality: 176 tests passing; mypy --strict clean; zero paid calls in the
  suite (the script's real Haiku calls only happen in a live demo).

Version 04.01.00 (2026-07-21)
------------------------------
Version v04.01 — Contrasting personas (the theater). First version of phase
v04 (Agent-vs-Agent Orchestration).
Git tag: opus-opus-v04.01.00 (namespaced — this branch's independent build).

- profiles/aggressive.yml (Ironclaw, refined): sharpened toward
  forcing/double-threat play, temperature 0.85 -> 0.9. (ARENA-OPUS-OPUS-032)
- profiles/cautious.yml (new — Bastion): wary block-first defender; calm,
  dry tone; temperature 0.4, memory 6 -- contrasts Ironclaw on every axis.
  Both drive the real create_llm_client. (ARENA-OPUS-OPUS-032)
- Review: no findings.
- Quality: 170 tests passing; mypy --strict clean; zero paid calls.

Version 03.03.00 (2026-07-21)
------------------------------
Version v03.03 — Chat & the Observer role. **v03 release gate**: the Web UI
(Player & Observer) is now feature-complete.
Git tag: opus-opus-v03.03.00 (namespaced — this branch's independent build).

- renderChat/renderSystem: chat bubbles by seat symbol (self left/blue,
  other right/pink), textContent-escaped, auto-scrolling; system lines for
  match-start/game-over/errors (closes v03.01 review #2). Chat form posts
  non-optimistically. (ARENA-OPUS-OPUS-030)
- renderPlayers labels both cards "Observer" when mySymbol === null; board
  + chat already read-only for an Observer. End-to-end verified: an
  Observer join yields symbol:null and no seat. (ARENA-OPUS-OPUS-031)
- Review fix: the server now refuses a seatless connection's chat
  ("observers cannot chat"), matching submit_move's "no seat" guard --
  closing the observer-read-only-chat item v01.04 deferred here (MEDIUM).
- Quality: 167 tests passing; mypy --strict clean; zero paid calls.

Version 03.02.00 (2026-07-21)
------------------------------
Version v03.02 — Board & gameplay (Player role).
Git tag: opus-opus-v03.02.00 (namespaced — this branch's independent build).

- renderBoard/renderPlayers/renderTurnBanner: the board renders from
  joined/state_update; a cell is playable only on the human's legal turn
  (real disabled attribute); the current-turn card is highlighted.
  (ARENA-OPUS-OPUS-028)
- handleCellClick -> submit_move (never optimistic); handleGameOver
  freezes the board, derives + highlights the winning line client-side,
  shows the result; resetPlayState clears a second in-tab match.
  (ARENA-OPUS-OPUS-029)
- Review: no HIGH/MEDIUM; 1 LOW (generic opponent label) deferred to v05.
- Quality: 158 tests passing; mypy --strict clean; zero paid calls.

Version 03.01.00 (2026-07-21)
------------------------------
Version v03.01 — Static UI shell & connection. First version of phase v03
(Web UI).
Git tag: opus-opus-v03.01.00 (namespaced — this branch's independent build).

- /ui static mount (CWD-independent) + a no-store middleware matching /ui
  exactly (not a loose prefix). (ARENA-OPUS-OPUS-025)
- web/index.html + styles.css: the arena shell mirroring ui_prototype.html
  (glass tokens, real <button> cells, responsive collapse, reduced-motion),
  self-contained. (ARENA-OPUS-OPUS-026)
- web/app.js: lobby -> REST -> one WebSocket, full (untruncated) match-id
  display, and a routeEvent case per server event; window.arena for tests.
  (ARENA-OPUS-OPUS-027)
- Review: no findings.
- Quality: 150 tests passing; mypy --strict clean; zero paid calls.

Version 02.03.00 (2026-07-21)
------------------------------
Version v02.03 — Agent event loop & decision play. **v02 release gate**:
phase v02 (Agent Client) is complete.
Git tag: opus-opus-v02.03.00 (namespaced — this branch's independent build).

- Agent CLI: parse_args, load_environment (.env -> load_api_key, secret
  stays in the agent), join_match over httpx. (ARENA-OPUS-OPUS-022)
- choose_move: decide -> validate -> retry (MAX_MOVE_ATTEMPTS=3) ->
  random-legal fallback, so a model failure never stalls. (ARENA-OPUS-OPUS-023)
- AgentSession: acts only on its own turn (never the terminal null-turn
  update), records opponent moves + chat, finishes on game_over; run_agent
  + main() wire the live loop. Full game to game_over over the real server
  (LLM mocked), exactly 4 model calls proving no act on the terminal
  update. (ARENA-OPUS-OPUS-024)
- Review: no HIGH/MEDIUM; 1 LOW (bare-traceback CLI ergonomics) deferred
  to v05.
- Quality: 138 tests passing; mypy --strict clean; zero paid calls.

Version 02.02.00 (2026-07-21)
------------------------------
Version v02.02 — Profile, persona, memory & prompt.
Git tag: opus-opus-v02.02.00 (namespaced — this branch's independent build).

- AgentProfile YAML loader (extra=forbid, bounded temperature/memory,
  model_type feeds create_llm_client) + profiles/aggressive.yml
  (Ironclaw). (ARENA-OPUS-OPUS-019)
- MemoryWindow: a bounded rolling buffer of recent moves + chat.
  (ARENA-OPUS-OPUS-020)
- build_prompt: persona + memory + board + exactly-the-legal-moves into
  one decision prompt. (ARENA-OPUS-OPUS-021)
- Review fixes: opponent chat is now framed as never-instructions in the
  prompt (MEDIUM prompt-injection guard) and remembered chat is capped at
  160 chars (LOW) -- both with regression tests.
- Quality: 126 tests passing; mypy --strict clean; zero paid calls.

Version 02.01.00 (2026-07-21)
------------------------------
Version v02.01 — LLMClient seam & the Anthropic Haiku client. First
version of phase v02 (Agent Client).
Git tag: opus-opus-v02.01.00 (namespaced — this branch's independent build).

- AgentResponse{move, comment} + the abstract LLMClient seam (async,
  generic, server-free), pinned by a contract test. (ARENA-OPUS-OPUS-016)
- AnthropicHaikuClient: structured output via messages.parse
  (output_format) -> validated .parsed_output; model id claude-haiku-4-5.
  SDK always mocked in tests. (ARENA-OPUS-OPUS-017)
- create_llm_client config dispatch + fail-fast ANTHROPIC_API_KEY loading
  (never logged; stays in the agent process). (ARENA-OPUS-OPUS-018)
- Review: no findings (live-SDK call signature verified against the mock).
- Quality: 105 tests passing; mypy --strict clean; zero paid calls.

Version 01.04.00 (2026-07-21)
------------------------------
Version v01.04 — WebSocket protocol & move authority. **v01 release gate**:
phase v01 (Game Core & Server Foundation) is complete.
Git tag: opus-opus-v01.04.00 (namespaced — this branch's independent build).

- ConnectionManager + the five event builders + parse_action, pinning the
  §6.2 wire seam (serialize-once/prune broadcast). (ARENA-OPUS-OPUS-012)
- WS endpoint: token->4001, joined, and a finally cleanup with a shielded
  seat release (§10 cancellation path). (ARENA-OPUS-OPUS-013)
- chat action -> chat_message broadcast (persisted). (ARENA-OPUS-OPUS-014)
- submit_move §5.4 authority: no-seat/out-of-turn/illegal rejected;
  terminal state_update carries current_turn:null, then game_over +
  close_room; match row finished. Full win/draw games over two real WS
  connections. (ARENA-OPUS-OPUS-015)
- Review: no HIGH/MEDIUM; 2 LOW deferred (malformed-JSON -> v05.01,
  observer-read-only-chat -> v03.03).
- Quality: 89 tests passing; mypy --strict clean; zero paid calls.

Version 01.03.00 (2026-07-21)
------------------------------
Version v01.03 — Match, seats & lobby REST.
Git tag: opus-opus-v01.03.00 (namespaced — this branch's independent build).

- FastAPI app (create_app factory, lifespan runs init_models),
  GET /api/v1/health, per-request Repository dependency, lobby schemas.
  (ARENA-OPUS-OPUS-008)
- Auth tokens: issue_token (the opaque participant PK), IssuedToken,
  validate_token (None for unknown/cross-match) -- identity by token,
  never name. (ARENA-OPUS-OPUS-009)
- Lobby REST: POST /lobby/match (uuid) + POST /lobby/join (404 on unknown,
  writes is_spectator before any seat). (ARENA-OPUS-OPUS-010)
- match.py seat helpers over the Repository + the end-to-end identity
  proof: two "Human" joins get distinct X/O seats. (ARENA-OPUS-OPUS-011)
- Review: no findings.
- Quality: 64 tests passing; mypy --strict clean; zero paid calls.

Version 01.02.00 (2026-07-21)
------------------------------
Version v01.02 — Persistence layer (SQLite).
Git tag: opus-opus-v01.02.00 (namespaced — this branch's independent build).

- Async SQLAlchemy engine over sqlite+aiosqlite (FK pragma, NullPool) +
  the four ORM tables with UNIQUE(match_id, symbol) and an opaque JSON
  move column. (ARENA-OPUS-OPUS-004)
- Repository: the single access seam (matches/participants/moves/chat
  CRUD; opaque move payload persisted as-is). (ARENA-OPUS-OPUS-005)
- Seat rule (§5.2) keyed by token, built race-safe (retry on the UNIQUE
  guard) -- an asyncio race resolves to distinct seats. (ARENA-OPUS-OPUS-006)
- Game reconstruction by move-log replay + parity-derived current_turn
  (None at game-over). (ARENA-OPUS-OPUS-007)
- Review: no HIGH/MEDIUM; 1 LOW (double reconstruction in current_turn)
  deferred to v05.01.
- Quality: 47 tests passing; mypy --strict clean; zero paid calls.

Version 01.01.00 (2026-07-21)
------------------------------
Version v01.01 — Project skeleton & the game engine. First version of
phase v01 (Game Core & Server Foundation).
Git tag: opus-opus-v01.01.00 (namespaced — this branch's independent build).

- Repo skeleton: pyproject.toml (pytest + strict mypy, setuptools packages
  pinned to games/server/agent), the architecture §2 package layout, and a
  smoke test. (ARENA-OPUS-OPUS-001)
- GameInterface abstract seam + contract test pinning the four §4.1
  signatures. (ARENA-OPUS-OPUS-002)
- TicTacToe engine (sole legality authority, never raises) + exhaustive
  unit matrix: a win on each of the 8 lines, draw detection, per-category
  illegal rejection, the get_valid_moves shrink invariant. (ARENA-OPUS-OPUS-003)
- Review: no findings.
- Quality: 22 tests passing; mypy --strict clean; zero paid calls (no model
  involved this phase).
