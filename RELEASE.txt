AgentArena — Release Notes
==========================

Version 05.03.00 (2026-07-21)
-----------------------------
Version v05.03 — Polish & docs. THE v05 RELEASE GATE: the MVP is complete. A
newcomer can set up and run all three modes from the README. Shipped via
/ship-phase. Git tag: opus-v05.03.00.

- A real README (ARENA-OPUS-037): what AgentArena is, architecture at a glance +
  spec links, setup (venv, pip install -e ".[dev]", ANTHROPIC_API_KEY in .env), the
  three run modes with the actual commands (human-vs-agent, agent-vs-agent via
  scripts/run_arena.sh + Observe, observe), the WebSocket protocol summary, testing
  (pytest + mypy, LLM mocked, no paid calls), and data/reset (./arena.db). Replaces
  the 3-line stub that named the wrong vendors.
- A tracked .env.example (ANTHROPIC_API_KEY placeholder; .env stays gitignored).
- A doc-smoke test (tests/test_readme.py) that keeps the README honest: real
  commands/paths, referenced files exist, all three modes documented.
- UI polish: the "arena" aesthetic already shipped in v03 (styles.css mirrors the
  prototype) -- the DoD's clean UI was already met.
- Quality: 231 tests passing (deterministic); mypy --strict clean; zero paid calls.

Version 05.02.00 (2026-07-21)
-----------------------------
Version v05.02 — End-to-end integration suite. Shipped via /ship-phase.
Git tag: opus-v05.02.00.

- No-paid-call guard (ARENA-OPUS-035): an autouse conftest fixture patches
  agent.llm.AsyncAnthropic to a dummy for EVERY test, so no test path can open a
  real Anthropic network client -- even one that forgets to mock the SDK. The
  whole suite is paid-call-safe by construction.
- Agent-vs-agent end-to-end (ARENA-OPUS-036): two real AgentSessions (Blaze X +
  Bastion O, both LLMs mocked with a scripted game) play to game_over over the
  real server via two TestClient WebSockets; a lock-step broadcast driver, with
  LLM call counts proving no action on the terminal update. Deterministic 10/10.
- The DoD's other paths were already covered (full-game WS, human-vs-agent,
  observer, disconnect/reconnect, DB against a throwaway SQLite) -- the execution
  report maps each clause to its test.
- Quality: 212 tests passing (deterministic); mypy --strict clean; zero paid calls
  (now mechanically enforced).

Version 05.01.00 (2026-07-21)
-----------------------------
Phase v05 (Hardening & Polish) begins. Version v05.01 — resilience & reconnection,
shipped as a verification checkpoint. Git tag: opus-v05.01.00.

- The v05.01 resilience behaviours were already delivered by earlier HARDEN sweeps
  and fixes: seat release on disconnect under asyncio.shield (reclaimable), a
  finally-based cleanup that treats cancellation like a disconnect, broadcast that
  prunes a dead socket while still delivering to the rest, malformed JSON -> error
  (connection kept), missing/invalid token -> close 4001, and model errors -> a
  legal fallback move.
- This version closes the one coverage gap (ARENA-OPUS-034, test-only): a
  same-token reconnect reclaims its seat, and the two-seat invariant holds across a
  drop/reconnect cycle (no leak). The execution report maps each DoD clause to its
  covering test.
- Review: no HIGH/MEDIUM; the fast-reconnect release-ordering window is self-healing
  via assign_symbol (accepted, out of the checkpoint's scope).
- Quality: 209 tests passing (new reconnect tests deterministic 5/5); mypy --strict
  clean; no paid calls.

Version 04.02.00 (2026-07-21)
-----------------------------
Version v04.02 — Arena orchestration script. THE v04 RELEASE GATE: one command
starts a watchable agent-vs-agent match. Shipped via /ship-phase.
Git tag: opus-v04.02.00.

- scripts/run_arena.sh: from the repo root, creates a match (POST /lobby/match,
  match_id parsed with Python -- no jq), prints the full id + the Observe link with
  an explicit "use Observe, NOT Join", launches agent 1 (X, aggressive) and WAITS
  for its "joined as" line before launching agent 2 (O, cautious) so X/O is
  deterministic (seats are already race-safe server-side), trap-cleans up both
  agents on INT/TERM/EXIT, and tails both logs. Interpreter / SERVER_URL / profiles
  are overridable. (ARENA-OPUS-033)
- The test does static checks only (bash -n, references, wait-before-second
  ordering, Observe-not-Join, cleanup) -- it never executes the script, so the
  suite makes no paid calls.
- Quality: 207 tests passing (deterministic); mypy --strict clean.

Version 04.01.00 (2026-07-21)
-----------------------------
Phase v04 (Agent-vs-Agent Orchestration) begins. Version v04.01 — contrasting
personas: the theater. Shipped via /ship-phase. Git tag: opus-v04.01.00.

- profiles/aggressive.yml (refined): Blaze — an aggressive taunter (center/corner
  + double-threat play, one-line swaggering trash-talk, temperature 0.9).
- profiles/cautious.yml (new): Bastion — a wary defender (block-first, draw-seeking,
  calm/wry voice, temperature 0.4, memory_limit 6). A deliberate contrast to Blaze
  in name, tone, temperature, and memory, for an entertaining watched match.
- Both validate against the shipped AgentProfile and drive create_llm_client; the
  roster test asserts they load, are distinct (cautious temp < aggressive), and
  factory-dispatch -- with the SDK patched, no paid call.
- Quality: 201 tests passing (deterministic); mypy --strict clean; the seam is
  mocked in tests -- zero paid calls (the personas run live only in the v04.02 demo).

Version 03.03.00 (2026-07-21)
-----------------------------
Version v03.03 — Chat & the Observer role. THE v03 RELEASE GATE: the Web UI phase
is functionally complete — a human can host/join and play, chat, and watch an
agent-vs-agent match read-only in the browser. Shipped via /ship-phase.
Git tag: opus-v03.03.00.

- Chat (web/app.js): the form sends a `chat` action (never optimistic); chat_message
  renders a bubble built with textContent (untrusted content, no innerHTML), styled
  self-vs-other by seat symbol (sender === mySymbol); system lines for game over and
  surfaced errors. (ARENA-OPUS-030)
- Observer role (v03 gate): a joined with symbol:null renders a read-only view --
  cards "Observer", chat input suppressed, board never enabled, null-safe -- never
  claiming a seat. Proven end-to-end against the real server: no seat, forced
  submit_move/chat refused server-side. (ARENA-OPUS-031)
- Reconciliation: chat_message.sender is the seat symbol (not the player name);
  web_ui_specification §4.4 corrected to match the wire contract. v03.01 review #2
  (error surfacing) fixed here.
- Quality: 198 tests passing (deterministic); mypy --strict clean; the UI never
  touches a model -- zero paid calls.

Version 03.02.00 (2026-07-21)
-----------------------------
Version v03.02 — Board & gameplay (Player role). A human can now play a full game
in the browser. Shipped via /ship-phase. Git tag: opus-v03.02.00.

- renderBoard/renderPlayers/renderTurnBanner (web/app.js): the board renders
  entirely from joined/state_update; each cell is playable only when empty, the
  game is active, it's the human's turn, and the move is legal (real disabled
  attribute); the active player card is highlighted; null-safe for the Observer.
  (ARENA-OPUS-028)
- handleCellClick -> submit_move (never optimistic — the board re-renders from the
  resulting state_update); off-turn / finished / closed-socket / marked clicks are
  ignored. handleGameOver freezes the board, derives + highlights the winning line
  (none on a draw), and shows the result in status + banner. resetPlayState clears
  a second match in the same tab (the §7 no-reload regression guard). (ARENA-OPUS-029)
- Review: no HIGH/MEDIUM; opponent-name label deferred to v05 polish (the joined
  payload carries no participant names).
- Quality: 191 tests passing (deterministic); mypy --strict clean; the UI never
  touches a model -- zero paid calls.

Version 03.01.00 (2026-07-21)
-----------------------------
Phase v03 (Web UI — Player & Observer) begins. Version v03.01 — static UI shell &
connection. Shipped via /ship-phase. Git tag: opus-v03.01.00.

- Static Web UI served at /ui (StaticFiles html=True, dir resolved from the repo
  root) with Cache-Control: no-store on /ui/* so edits aren't masked by browser
  caching; non-/ui routes unaffected. (ARENA-OPUS-025)
- The "arena" shell (web/index.html + web/styles.css) mirroring the design source
  ui_prototype.html: app bar with the FULL (untruncated) match id, a 3x3 board of
  real <button> cells, player cards, and a chat panel; responsive single-column
  collapse and a prefers-reduced-motion guard; self-contained (system fonts, no
  CDN). (ARENA-OPUS-026)
- web/app.js: Host/Join/Observe -> REST lobby -> ONE WebSocket; a connection-status
  indicator; setMatchIdDisplay shows the full id (no truncation anywhere); a
  routeEvent dispatcher with a case per server event (board/chat rendering land in
  v03.02/03); isGameActive reset guard for a second match in the same tab.
  (ARENA-OPUS-027)
- Review: no HIGH/MEDIUM; the no-store middleware now matches /ui exactly (was a
  loose prefix match); error-surfacing deferred to v03.03.
- Quality: 184 tests passing (deterministic); mypy --strict clean; the UI never
  touches a model -- zero paid calls.

Version 02.03.02 (2026-07-21)
-----------------------------
Tooling-only patch — no server/agent/game behavior change.
Git tag: opus-v02.03.02.

- scripts/play.py: a manual terminal play/observe client for local testing until
  the Web UI arrives in v03. Joins over REST, drives one WebSocket, renders the
  board, prints moves/chat, prompts for a move on your turn, handles game_over.
  Pure external client (httpx + websockets; imports nothing from server/). Used
  for manual agent-vs-human and agent-vs-agent runs.

Version 02.03.01 (2026-07-21)
-----------------------------
Post-release hardening patch — the end-of-phase HARDEN sweep of /ship-phase v02
--harden closing the remaining MEDIUM code-review findings (v01.04 #2 and #4).
Git tag: opus-v02.03.01.

- Malformed WebSocket JSON (or a non-object message) now yields an `error` event
  and KEEPS the connection, instead of tearing the socket down. (review #4)
- The WS cleanup releases a dropped player's seat under asyncio.shield, so a
  client-initiated drop (async cancellation) can no longer leak the seat; a
  reconnecting or new participant reclaims it. (review #2)
- Both fixes ship regression tests (garbage-input survival; drop-and-reclaim).
  These pre-address the corresponding v05.01 resilience tasks.
- Quality: 174 tests passing (deterministic); mypy --strict clean; zero paid calls.

Version 02.03.00 (2026-07-21)
-----------------------------
Version v02.03 — Agent event loop & decision play. THE v02 RELEASE GATE: phase
v02 (Agent Client — Haiku) is functionally complete. Shipped via /ship-phase.
Git tag: opus-v02.03.00 (namespaced — the independent Opus build).

- Runnable agent CLI: `python agent/agent.py --match-id <id> --profile
  profiles/aggressive.yml` — .env key loading (python-dotenv; closes review #7),
  profile -> create_llm_client, REST join, WS session, clean exit on room close.
  Runtime deps: httpx, websockets, python-dotenv. (ARENA-OPUS-022)
- choose_move: decide -> validate against valid_moves -> retry (<=3, with the
  rejection fed back into the retry prompt) -> random-LEGAL fallback, so a
  hallucinated move never stalls the match. (ARENA-OPUS-023)
- AgentSession + run loop: acts only when current_turn == its symbol (never on
  the terminal state_update or as an observer), records opponent moves + chat
  into the bounded memory window, sends one {chat, submit_move} per turn.
  Integration-proven against the real server with a scripted mock LLM, incl.
  the illegal->retry path. (ARENA-OPUS-024)
- Review: no HIGH/MEDIUM findings; reconnect stays v05.01 scope.
- Quality: 172 tests passing (deterministic); mypy --strict clean; the LLM is
  mocked in every test — zero paid calls.

Version 02.02.00 (2026-07-21)
-----------------------------
Version v02.02 — Profile, persona, memory & prompt: the MVP Agent Designer
surface. Shipped via the /ship-phase pipeline (generate -> upload -> execute ->
review-and-fix -> release).
Git tag: opus-v02.02.00 (namespaced — the independent Opus build).

- AgentProfile (agent/profile.py): a strictly-typed persona/model/memory config
  loaded from YAML — name, system_prompt, model_type (feeds create_llm_client;
  default "haiku"), temperature (0..1), memory_limit (>0); unknown keys rejected;
  clear errors for missing/invalid/non-mapping files. First sample persona:
  profiles/aggressive.yml ("Blaze"). (ARENA-OPUS-019)
- MemoryWindow (agent/memory.py): a bounded rolling window of recent moves +
  chat, oldest evicted first; events() returns an oldest-first copy.
  (ARENA-OPUS-020)
- build_prompt (agent/prompt.py): persona + recent events + the pushed board
  (empty cells shown by index) + legal moves -> one decision prompt whose reply
  contract matches AgentResponse{move, comment}. (ARENA-OPUS-021)
- Code review (same cycle): opponent chat is now explicitly framed as banter,
  never instructions (prompt-injection guard), and remembered chat is length-
  bounded so a huge message can't bloat later prompts. Server-side chat length
  cap recorded for v05.01.
- Quality: 159 tests passing (deterministic); mypy --strict clean; no model call
  anywhere in code or tests — zero paid calls.

Version 02.01.00 (2026-07-21)
-----------------------------
Phase v02 (Agent Client — Haiku) begins. Version v02.01 — the LLMClient seam &
the Anthropic Haiku client. The model is introduced behind a vendor-agnostic
seam and is always mocked in tests: zero paid calls.
Git tag: opus-v02.01.00 (namespaced — the independent Opus build).

- LLMClient seam (agent/llm.py): the abstract, vendor-agnostic client — one async
  generate_structured_response(prompt, schema) returning a validated instance —
  plus AgentResponse{move, comment} (agent/schemas.py). Contract test pins the
  seam; agent/ imports nothing from server/. (ARENA-OPUS-016)
- AnthropicHaikuClient: the only vendor implementation, via the async Anthropic
  SDK; structured output forced with messages.parse(output_format=schema) and
  returned validated (raises if absent). Model id is a single constant,
  claude-haiku-4-5. (ARENA-OPUS-017)
- create_llm_client(model_type, api_key, temperature) config-driven dispatch +
  fail-fast ANTHROPIC_API_KEY handling; the key lives only in the agent process
  and is never logged. (ARENA-OPUS-018)
- Robustness (from the v01 code review, fixed in this window): race-safe seat
  assignment (UNIQUE-guard retry — two simultaneous connects now get X and O),
  observers can no longer post chat, empty player_name rejected with 400; the
  SQLite engine runs on NullPool for a deterministic test suite.
- Quality: 132 tests passing (deterministic); mypy --strict clean; no paid model
  call anywhere.

Version 01.04.00 (2026-07-20)
-----------------------------
Phase v01.04 — WebSocket protocol & move authority. This completes v01: two raw
WebSocket clients can now play a full, server-validated Tic-Tac-Toe game with chat
to game_over.
Git tag: opus-v01.04.00 (namespaced — this is the independent Opus build).

- WebSocket protocol layer (server/websockets.py): the ConnectionManager (per-match
  sockets + owner map, a serialize-once broadcast that prunes a dead socket, send_to,
  close_room) and pure builders for the five server->client events (joined,
  state_update, chat_message, game_over, error) plus inbound {action, payload} parsing.
  (ARENA-OPUS-012)
- WS endpoint: GET /ws/match/{id}?token= validates the token (bad/missing -> close 4001),
  connects, claims/reports the seat, sends joined, runs the receive loop, and cleans up
  in a finally block (disconnect + release_seat). (ARENA-OPUS-013)
- Chat: a chat action persists the message and broadcasts chat_message to the room.
  (ARENA-OPUS-014)
- Move authority (server/main.py, §5.4): every submit_move is re-validated server-side --
  seat, turn, and legality. Illegal / out-of-turn / no-seat moves get an error. A legal
  move persists and broadcasts state_update (current_turn null on the game-ending move,
  with last_move); on game over the server broadcasts game_over and closes the room, and
  marks the match finished with its result (Repository.finish_match). (ARENA-OPUS-015)
- Fix: gate the WS receive loop on the socket being connected so close_room after
  game_over no longer raises on a closed socket (found via a live uvicorn game).
- Quality: 116 tests passing; mypy --strict clean; no model touched and no paid calls
  anywhere (the LLMClient seam arrives in v02.01).

Version 01.03.00 (2026-07-20)
-----------------------------
Phase v01.03 — Match, seats & lobby REST.
Git tag: opus-v01.03.00 (namespaced — the shared repo's v01.03.00 tag belongs to a
sibling branch; this is the independent Opus build).

- FastAPI app: server/main.py builds the app via create_app(...) with a lifespan that
  runs init_models on startup, GET /api/v1/health, and a per-request Repository
  dependency; server/schemas.py adds the lobby request/response models. The app is now
  runnable with `uvicorn server.main:app`. (ARENA-OPUS-008)
- Auth tokens: server/auth.py issues an opaque token that IS the participant_id, with the
  frozen IssuedToken value object and validate_token (None for unknown/other-match tokens).
  Identity is keyed by token, never by display name. (ARENA-OPUS-009)
- Lobby REST: POST /api/v1/lobby/match creates a match and returns its id; POST
  /api/v1/lobby/join returns 404 for an unknown match, else records the participant
  (carrying is_spectator) and returns the token. (ARENA-OPUS-010)
- Seat helpers: server/match.py provides thin assign_symbol / release_seat wrappers over
  the Repository. End-to-end tests prove the seat-by-token identity against real join
  tokens: two players get X/O, a third gets no seat, a spectator is never seated, and two
  joins named "Human" get distinct seats. (ARENA-OPUS-011)
- Quality: 90 tests passing; mypy --strict clean; no model touched and no paid calls
  anywhere (lobby is REST + DB only; the LLMClient seam arrives in v02.01).

Version 01.02.00 (2026-07-19)
-----------------------------
Phase v01.02 — Persistence layer (SQLite).
Git tag: opus-v01.02.00 (namespaced — the shared repo's v01.02.00 tag belongs to a
sibling branch; this is the independent Opus build).

- Async SQLite persistence: an async SQLAlchemy engine over sqlite+aiosqlite (config-driven
  URL), async_sessionmaker(expire_on_commit=False), a connect-time PRAGMA foreign_keys=ON,
  and init_models(). The four ORM tables (matches, participants, moves, chat_messages) with
  match_id foreign keys and UNIQUE(match_id, symbol); opaque move payloads stored as JSON so
  their type round-trips. (ARENA-OPUS-004)
- Repository: the single access seam for durable state — create_match, get_match,
  add_participant, log_move, log_chat. State survives a process restart. (ARENA-OPUS-005)
- Seat rule (§5.2): assign_symbol / release_seat, keyed by token not display name — spectators
  never get a seat; a third player gets none; reconnects are idempotent. (ARENA-OPUS-006)
- Reconstruction: reconstruct_game replays the ordered move log through a fresh TicTacToe (no
  serialize step on GameInterface); current_turn is move-count parity, None once over.
  (ARENA-OPUS-007)
- Quality: 72 tests passing; mypy --strict clean; no model touched and no paid calls anywhere
  (persistence is pure server/DB logic; the LLMClient seam arrives in v02.01).

Version 01.01.00 (2026-07-19)
-----------------------------
Phase v01.01 — Project skeleton & the game engine.
Git tag: opus-v01.01.00 (namespaced — the shared repo's v01.01.00 tag belongs to a
sibling branch; this is the independent Opus build).

- Project skeleton: pyproject.toml (pytest + strict mypy config) and the architecture
  §2 package layout (games/server/agent packages; web/profiles/scripts placeholders),
  with a smoke test proving the toolchain runs and the packages import. (ARENA-OPUS-001)
- GameInterface seam: the abstract game plug-in — get_state, get_valid_moves, apply_move,
  is_game_over — with a contract test pinning the §4.1 method signatures and abstractness.
  (ARENA-OPUS-002)
- TicTacToe engine behind the seam: 9-cell board, X first, 8 winning lines, "draw" on a
  full board; apply_move is the sole legality authority and never raises on bad input
  (out-of-range / occupied / wrong-type). Ships an exhaustive unit suite (a win on every
  line, draw detection, illegal rejection per category, valid-moves shrink invariant).
  (ARENA-OPUS-003)
- Quality: 43 tests passing; mypy --strict clean on source and tests; no paid model calls
  anywhere (pure logic — the LLMClient seam arrives in v02.01).
