Version v05.01.00 (2026-07-18)
---------------------------
- Engineered `AgentProfile` Pydantic class to standardize configuration schemas.
- Implemented declarative YAML parsing with `PyYAML`.
- Authored the first two dynamic AI personas (`aggressive_bot.yml` and `cowardly_bot.yml`) inside the new `profiles/` directory.

Version v04.03.00 (2026-07-18)
---------------------------
- Added dynamic Board Reactivity to visually parse `state_update` payloads into the DOM Grid.
- Bound empty grid cells to emit `submit_move` WebSocket events (only when it is the human's turn).
- Fully implemented the interactive Chat Ledger to display Agent vs Human messages.
- Handled End of Game triggers (win/loss/draw) to freeze the board and announce the winner.
- Fixed a backend bug where the Tic-Tac-Toe state incorrectly emitted `status: None` during an active game.

Version v04.02.00 (2026-07-18)
---------------------------
- Connected Web UI to FastAPI backend via WebSockets.
- Implemented `/api/v1/lobby/match` and `/api/v1/lobby/join` HTTP calls in Vanilla JS.
- Established native `ws.onmessage` event router to parse JSON push events.
- Added `/ui` static file mount to FastAPI so the backend can serve the frontend directly.

Version v04.01.00 (2026-07-18)
---------------------------
- Designed premium dark-mode Glassmorphism UI aesthetic using backdrop-filters and translucent panels.
- Constructed semantic HTML5 layout separating Game Area and Chat Sidebar.
- Scaffolod responsive CSS Grid for Tic-Tac-Toe board and Flexbox for message ledger.
- Added drop-shadow neon accents for active human and agent game elements.

Version v03.03.00 (2026-07-17)
---------------------------
- Agent uses `google-genai` Structured Outputs (`response_schema`) to reliably extract JSON moves and comments.
- Agent validates its parsed moves and uses an automatic bounded retry loop for hallucinations.
- Agent successfully sends `submit_move` and `chat` JSON payloads over the WebSocket to complete the game loop.
- Agent correctly ceases generating moves when `status` is not `ACTIVE`.

Version v03.02.00 (2026-07-17)
---------------------------
- Integrated `google-genai` SDK and created `LLMClient` wrapper for `gemini-3.1-pro-preview`.
- Added `MemoryWindow` to Agent CLI to track chat history and game events.
- Implemented Prompt Builder with arrogant persona injection.
- Agent successfully generates context-aware LLM responses when it is its turn.

Version v03.01.00 (2026-07-17)
---------------------------
- Added Agent CLI skeleton with `argparse` configuration.
- Added automatic Lobby Token retrieval over HTTP using `aiohttp`.
- Added resilient `websockets` loop to natively consume Match events in Python.
- Configured `python-dotenv` for upcoming Agent credentials.

Version v02.03.00 (2026-07-17)
---------------------------
- Integrated the Tic-Tac-Toe Game Engine with the WebSocket Connection Manager.
- Server now validates `submit_move` payloads and strictly enforces turn logic.
- Implemented robust `state_update` broadcasting and isolated `error` handling for out-of-turn play.
- Emits `game_over` signals upon win/draw and elegantly handles socket teardown.
- Upgraded the `test_client.html` with a clickable UI and multiplayer support for easy end-to-end testing.

Version v02.02.00 (2026-07-17)
---------------------------
- Created the Tic-Tac-Toe Engine logic in `games/tictactoe.py`.
- Wrote rigorous state and bounds checking tests for all 8 win permutations and draw limits in `tests/test_tictactoe.py`.

Version v02.01.00 (2026-07-17)
---------------------------
- Defined the strict abstract base class `GameInterface` for future engines.
- Configured MyPy strict type validation for the `games/` module.

Version v01.03.00 (2026-07-17)
---------------------------
- docs: add test_client.html and update README with WS testing instructions
- ARENA-011: Concurrent WebSocket Tests
- ARENA-010: Event Routing & Broadcast Loop
- ARENA-009: WebSocket Endpoint & Auth Handshake
- ARENA-008: WebSocket Schemas & ConnectionManager

Version v01.02.00 (2026-07-17)
---------------------------
- Configured foundational `aiosqlite` async database engine and declarative Base.
- Defined primary SQLAlchemy schema models (`MatchModel`, `MoveLogModel`, `ChatLogModel`).
- Implemented `ArenaRepository` for decoupled CRUD persistence logic.
- Added strict foreign key constraint handling and pytests.
- Added database initialization and seeding script `server/init_db.py`.

Version v01.01.00 (2026-07-17)
---------------------------
- Defined Game Server modular architecture and phase-based roadmap specifications.
- Configured autonomous IDE agent skills (generate, upload, execute, release).
- Scaffolded FastAPI server skeleton with CORS and Health endpoint.
- Implemented /api/v1/lobby/match for unique game generation.
- Implemented /api/v1/lobby/join for issuing secure Auth Tokens to players.
- Added foundational tests with pytest and locked dependencies.
