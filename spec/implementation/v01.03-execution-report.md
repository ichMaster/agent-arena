# Phase v01.03 — Execution Report

**Date:** 2026-07-20
**Branch:** Anthropic-Opus4.8-dev
**Label:** v01.03::phase
**Target release:** v01.03.00
**Executed by:** Claude Code (Opus 4.8)

## Summary

| Status | Count |
|--------|-------|
| Completed | 4 |
| Failed | 0 |
| Skipped | 0 |
| Remaining | 0 |

All four issues implemented, validated (pytest + strict mypy), committed, pushed, and closed in
dependency order. Every line was generated fresh in-session — no code copied from any sibling branch.

## Issues

| # | ARENA ID | GH | Title | Phase | Status | Commit | Files | Tests |
|---|----------|----|-------|-------|--------|--------|-------|-------|
| 1 | ARENA-OPUS-008 | #42 | FastAPI app, lifespan, health & lobby schemas | v01.03 | completed | fa886ca | 4 | pass (74) |
| 2 | ARENA-OPUS-009 | #43 | Auth tokens (auth.py) & the seat-by-token identity | v01.03 | completed | aba3aa7 | 3 | pass (81) |
| 3 | ARENA-OPUS-010 | #44 | Lobby REST — create & join | v01.03 | completed | 6f8106c | 2 | pass (85) |
| 4 | ARENA-OPUS-011 | #45 | Seat helpers (match.py) + end-to-end seat identity | v01.03 | completed | e430c8d | 2 | pass (90) |

*(Test counts are the full-suite totals after each issue: 74 → 81 → 85 → 90.)*
A preceding docs commit `ff612f2` recorded the phase planning artifacts (issues file + GitHub report).

## Detailed Results

### ARENA-OPUS-008: FastAPI app, lifespan, health & lobby schemas
**Status:** completed · **Commit:** fa886ca · **GitHub:** #42 (closed)
**Files:** `pyproject.toml`, `server/main.py`, `server/schemas.py`, `tests/test_app.py`
**What:** `create_app(db_engine=, session_maker=)` builds the app with a `lifespan` running `init_models`, `GET /api/v1/health`, and the `get_repository` dependency (per-request Repository over `app.state.session_maker`). Lobby schemas. `fastapi` runtime + `httpx` dev deps. `/ui` mount + CORS deferred to v03.
**Validation:** [x] pytest (74) · [x] mypy strict (10 files) · [x] acceptance

### ARENA-OPUS-009: Auth tokens (auth.py) & the seat-by-token identity
**Status:** completed · **Commit:** aba3aa7 · **GitHub:** #43 (closed)
**Files:** `server/auth.py`, `server/repository.py`, `tests/test_auth.py`
**What:** `issue_token()` (opaque id == participant PK), frozen `IssuedToken`, `validate_token(repo, match_id, token)`; `Repository.get_participant`. Contract: identity keyed by token, not name.
**Validation:** [x] pytest (81) · [x] mypy strict (11 files) · [x] acceptance

### ARENA-OPUS-010: Lobby REST — create & join
**Status:** completed · **Commit:** 6f8106c · **GitHub:** #44 (closed)
**Files:** `server/main.py`, `tests/test_lobby.py`
**What:** `POST /api/v1/lobby/match` (uuid match) and `POST /api/v1/lobby/join` (404 on unknown match; else `issue_token` + `add_participant`, returns the token). Integration: happy path, 404, spectator flagged.
**Validation:** [x] pytest (85) · [x] mypy strict (11 files) · [x] acceptance

### ARENA-OPUS-011: Seat helpers (match.py) + end-to-end seat identity
**Status:** completed · **Commit:** e430c8d · **GitHub:** #45 (closed)
**Files:** `server/match.py`, `tests/test_match_identity.py`
**What:** thin `assign_symbol`/`release_seat` over the Repository (§5.2). End-to-end tests drive the lobby over `TestClient` (real tokens) then assign seats on a separate engine bound to the same DB file: two tokens → X/O, third → none, spectator → never seated, two `"Human"` → distinct, release → reclaim.
**Validation:** [x] pytest (90) · [x] mypy strict (12 files) · [x] acceptance

## Phase DoD (roadmap §v01.03)

> Creating a match then joining returns a token; joining an unknown match 404s; two distinct tokens
> get X and O, a third player gets no seat, and a `spectator:true` join can never be assigned a
> seat — even two joins sharing the display name `"Human"` get distinct seats.

**Met.** Contract pinned this phase: the seat-by-token identity model (§5.2/§6.3) — token == `participant_id`,
identity keyed by token not name — via the contract test (#43) and the end-to-end identity test (#45).
Implemented as documented, so no architecture.md change. No model touched; no paid API call anywhere.

## Next Steps

- Phase v01.03 is complete — all issues closed, suite green (90 tests), types strict-clean.
- **Release (optional, requires confirmation):** `/release-version v01.03.00` (namespaced tag
  `opus-v01.03.00`, since the shared repo already holds `v01.03.00`).
- **Next phase:** `/generate-issues v01.04` — WebSocket protocol & move authority (the
  `ConnectionManager`, the `/ws/match/{id}` endpoint, the event/action protocol, the §5.4 move
  authority flow). **This is the v01 release gate.**
