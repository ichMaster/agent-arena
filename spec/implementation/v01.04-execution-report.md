# Phase v01.04 — Execution Report

**Date:** 2026-07-20
**Branch:** Anthropic-Opus4.8-dev
**Label:** v01.04::phase
**Target release:** v01.04.00
**Executed by:** Claude Code (Opus 4.8)

## Summary

| Status | Count |
|--------|-------|
| Completed | 4 |
| Failed | 0 |
| Skipped | 0 |
| Remaining | 0 |

All four issues implemented, validated (pytest + strict mypy), committed, pushed, and closed in
dependency order. Every line was generated fresh in-session — no code copied from any sibling branch.
**This phase clears the v01 release gate.**

## Issues

| # | ARENA ID | GH | Title | Phase | Status | Commit | Files | Tests |
|---|----------|----|-------|-------|--------|--------|-------|-------|
| 1 | ARENA-OPUS-012 | #46 | ConnectionManager, WS envelopes & event/action protocol | v01.04 | completed | af8b578 | 2 | pass (104) |
| 2 | ARENA-OPUS-013 | #47 | WS endpoint — connect, token auth, joined & finally cleanup | v01.04 | completed | 068a295 | 2 | pass (110) |
| 3 | ARENA-OPUS-014 | #48 | chat action → chat_message broadcast | v01.04 | completed | df99063 | 2 | pass (111) |
| 4 | ARENA-OPUS-015 | #49 | submit_move authority flow + game_over / close_room | v01.04 | completed | e450711 | 3 | pass (116) |

*(Test counts are the full-suite totals after each issue: 104 → 110 → 111 → 116.)*
A preceding docs commit `f852e1e` recorded the phase planning artifacts (issues file + GitHub report).

## Detailed Results

### ARENA-OPUS-012: ConnectionManager, WS envelopes & the event/action protocol
**Status:** completed · **Commit:** af8b578 · **GitHub:** #46 (closed)
**Files:** `server/websockets.py`, `tests/test_ws_protocol.py`
**What:** the pure, DB-free WS layer — `ConnectionManager` (per-match sockets + owner map; serialize-once broadcast with dead-socket prune; `send_to`; `close_room`) and pure builders for the five events + `parse_action`. Contract test pins every §6.2 shape.
**Validation:** [x] pytest (104) · [x] mypy strict (13 files) · [x] contract (§6.2) · [x] acceptance

### ARENA-OPUS-013: WS endpoint — connect, token auth, joined & finally cleanup
**Status:** completed · **Commit:** 068a295 · **GitHub:** #47 (closed)
**Files:** `server/main.py`, `tests/test_ws_endpoint.py`
**What:** `GET /ws/match/{id}?token=` — token→`4001`, connect, seat claim + `joined`, receive-loop dispatch, and a `finally` that disconnects + releases the seat (§10). Integration: joined X/O/spectator, `4001`, clean reconnect.
**Validation:** [x] pytest (110) · [x] mypy strict (13 files) · [x] acceptance
**Scope note:** the "reclaim a dropped seat by a *different* participant" sub-criterion surfaced the async-cancellation cleanup path (§10) and was **deferred to v05.01** (roadmap §v05.01: "a client-initiated drop still runs cleanup (the cancellation path)... a new connection reclaims it"). The `finally` cleanup mechanism the v01.04 DoD requires is in place.

### ARENA-OPUS-014: chat action → chat_message broadcast
**Status:** completed · **Commit:** df99063 · **GitHub:** #48 (closed)
**Files:** `server/main.py`, `tests/test_ws_chat.py`
**What:** the `chat` branch — persist via `log_chat`, broadcast `chat_message{sender, message}` to the room. Integration: two clients, one chats, both receive + persisted.
**Validation:** [x] pytest (111) · [x] mypy strict (13 files) · [x] acceptance

### ARENA-OPUS-015: submit_move authority flow + game_over / close_room
**Status:** completed · **Commit:** e450711 · **GitHub:** #49 (closed)
**Files:** `server/main.py`, `server/repository.py`, `tests/test_ws_game.py`
**What:** the §5.4 authority flow (assign → turn check → apply → persist → finish), `state_update` (terminal `current_turn: null` + `last_move`), `game_over` + `close_room`, `Repository.finish_match`, and `error` rejections. Integration: full win + draw, out-of-turn / illegal / no-seat rejections, terminal `current_turn: null`, room closed, match `finished`.
**Validation:** [x] pytest (116) · [x] mypy strict (13 files) · [x] acceptance

## Phase DoD (roadmap §v01.04) — **the v01 release gate**

> Two raw WS clients connect to one match, alternate server-validated legal moves, exchange chat, and
> reach `game_over` (room then closed); illegal, out-of-turn, and no-seat moves are rejected; the
> terminal `state_update` reports `current_turn: null`.

**Met.** Contract pinned this phase: the WebSocket event/action protocol (§6.2), via the 012 contract
test. Implemented as documented, so no architecture.md change. No model was touched (real-time layer
is server/WS/DB only). No paid API call anywhere.

**Known benign noise:** WS tests emit `ResourceWarning`/GC messages from undisposed async engines
under the client-drop cancellation path — tests are deterministic and green; the cancellation-safe
cleanup is hardened in v05.01.

## Next Steps

- Phase v01.04 is complete — all issues closed, suite green (116 tests), types strict-clean. **v01 is
  functionally complete.**
- **Release (optional, requires confirmation):** `/release-version v01.04.00` (namespaced tag
  `opus-v01.04.00`).
- **Next version:** `/generate-issues v02.01` — the `LLMClient` seam & the Anthropic Haiku client
  (the first phase that introduces a model, always mocked in tests).
