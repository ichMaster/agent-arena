# Phase v01.04 — Execution Report

**Date:** 2026-07-21 · **Branch:** Anthropic-Opus4.8-Opus4.8-dev · **Source:** `spec/implementation/v01.04-issues.md`
**Target release:** v01.04.00 (tag `opus-opus-v01.04.00`) · **Executed by:** Claude Opus 4.8, via `/ship-solution`

File-driven execution — no GitHub involved. Commit-only (no push). Every line generated fresh. **v01 release gate.**

## Summary

| Status | Count |
|--------|-------|
| Completed | 4 |
| Failed / Skipped / Remaining | 0 |

## Issues

| # | ARENA ID | Title | Status | Commit | Tests (suite) |
|---|----------|-------|--------|--------|---------------|
| 1 | ARENA-OPUS-OPUS-012 | `ConnectionManager`, WS envelopes & the event/action protocol | completed | 4a11a8c | 75 |
| 2 | ARENA-OPUS-OPUS-013 | WS endpoint — connect, token auth, `joined` & `finally` cleanup | completed | 1fcd45c | 82 |
| 3 | ARENA-OPUS-OPUS-014 | `chat` action → `chat_message` broadcast | completed | d21dee7 | 84 |
| 4 | ARENA-OPUS-OPUS-015 | `submit_move` authority flow + `game_over` / `close_room` | completed | 05a5d17 | 89 |

Suite grew 64 → 75 → 82 → 84 → 89; `mypy --strict` clean (13 source files) throughout.

## Highlights

- **Protocol layer (pure/DB-free):** the five event builders + `parse_action` pin the §6.2 wire seam;
  `ConnectionManager.broadcast` serializes once and prunes a socket that fails mid-send.
- **Endpoint + §10 cleanup:** token → 4001 on bad/missing; a single `joined` derived from the move
  log; cleanup in a `finally` with `await asyncio.shield(release_seat(...))`. As the reconcile flagged,
  this shield is core to §10's own guarantee — the TestClient WS-context-exit surfaces as task
  cancellation, and without the shield it would interrupt the release mid-commit and leak the seat.
  The reclaim-after-drop test passed deterministically 3/3.
- **Move authority (§5.4):** re-validated server-side end to end — no-seat/observer, out-of-turn, and
  illegal moves each yield an `error` without changing state; the terminal `state_update` carries
  `current_turn: null`, then `game_over` + `close_room`; the match row is set `finished`+`result`.
- Full games to a **win** and a **draw** over two real WS connections are covered.

## DoD (roadmap §v01.04, v01 release gate)

> Two raw WS clients connect to one match, alternate server-validated legal moves, exchange chat, and
> reach `game_over` (room then closed); illegal, out-of-turn, and no-seat moves are rejected; the
> terminal `state_update` reports `current_turn: null`.

**Met** — `test_ws_game.py` covers the full win/draw games and all three rejections with the terminal
`current_turn: null`; `test_ws_chat.py` covers chat exchange; `test_ws_endpoint.py` covers the
connection lifecycle + reclaim.

## Next Steps
`review-and-fix-issues v01.04` → `release-version v01.04.00` (tag `opus-opus-v01.04.00`, no push, **v01
release gate**) → phase v01 HARDEN sweep → v02.01 (the `LLMClient` seam).
