# v04.02 — Execution Report

**Phase:** v04.02 — Native WebSockets & Authentication
**Start:** 2026-07-18T20:18:23Z
**End:** 2026-07-18T20:21:15Z
**Duration:** 2m52s

## What was done

| Issue | Title | Commit |
|-------|-------|--------|
| ARENA-028 | Implement Lobby HTTP Integration (Match & Join) | `c1f1361` |
| ARENA-029 | Establish Native WebSocket Connection & Event Router | `ac9377f` |

- `web/app.js` (new): `hostMatch()` (POST `/api/v1/lobby/match`), `joinExistingMatch()` (prompts for a Match ID), both funneling into `promptAndJoin()` (POST `/api/v1/lobby/join`, stores the returned token). `connectSocket()` opens a real `WebSocket` at `/ws/match/{id}?token=...` using the page's own protocol (`ws`/`wss`); `onopen`/`onclose` toggle the `#connection-dot`'s `.connected` class; `onmessage` parses JSON and hands it to `routeEvent`, a dispatcher covering the five real server event types.
- `web/index.html`: links `app.js`.

Deliberately built and committed as two separate, correctly-scoped diffs — `app.js` was first written HTTP-only for ARENA-028, then extended with the WebSocket layer for ARENA-029, rather than authoring the whole file at once and splitting it artificially after the fact.

## Validation

| Check | Result |
|-------|--------|
| `node --check web/app.js` | syntax OK (both stages) |
| `pytest tests/test_static_ui.py` | 3 passed — confirms `/ui/app.js` is served and contains the expected functions at each stage |
| Full regression: `pytest tests/` | 95 passed |

No JS unit-test framework exists in this stack (Vanilla JS, no Node/npm build per the architecture spec), so validation here is syntax-checking + content assertions against the served file, consistent with v04.01's approach. Not verified in an actual browser (no browser-automation tool available in this environment) — worth a manual spot-check.

## Acceptance criteria

- [x] `app.js` contains HTTP fetch logic for `/match` and `/join`.
- [x] Button clicks trigger the network requests.
- [x] Match ID reflected in `#match-id-display`.
- [x] Browser connects via `ws://`/`wss://` depending on page protocol.
- [x] Server Status indicator turns green on connect, red on disconnect.
- [x] Incoming JSON payloads parsed and routed by event type in `onmessage`.
- [x] Phase DoD: the browser establishes a healthy WebSocket connection, toggling the Server Status dot.
