# Phase v04.02 — Execution Report

**Date:** 2026-07-21 · **Branch:** Anthropic-Opus4.8-dev · **Label:** v04.02::phase
**Target release:** v04.02.00 · **Executed by:** Claude Code (Opus 4.8), via /ship-phase v04

## Summary
| Status | Count |
|--------|-------|
| Completed | 1 |
| Failed / Skipped / Remaining | 0 |

**This version is the v04 release gate — met.**

## Issues
| # | ARENA ID | GH | Title | Commit | Tests (suite) |
|---|----------|----|-------|--------|---------------|
| 1 | ARENA-OPUS-033 | #67 | scripts/run_arena.sh — launch an agent-vs-agent match (v04 gate) | 1705da7 | 207 |

Planning docs: 1829aa9. Suite 201 → 207; mypy --strict clean (19 files).

## Highlights
- `scripts/run_arena.sh` (executable, tracked 100755): from the repo root, creates a match, prints the
  full id + the **Observe** link (with an explicit "use Observe, NOT Join"), launches agent 1 (X,
  aggressive) and **waits for its `joined as` line** before launching agent 2 (O, cautious) so X/O is
  deterministic, trap-cleans up both processes on exit, and `tail -f`s both logs.
- Determinism note: seats are already race-safe server-side (v01 #1); the wait-for-join only fixes
  which persona is X vs O. Interpreter / SERVER_URL / profiles are overridable.
- The test does **static checks only** (bash -n, references, wait-before-second ordering,
  Observe-not-Join, cleanup) — it never runs the script, so the suite makes **no paid calls**.

## DoD (roadmap §v04.02)
> ./scripts/run_arena.sh starts a full agent-vs-agent match; a human opening /ui → Observe with the
> printed id watches the whole game + banter to game_over; the two agents reliably get distinct seats.

**Met** — the orchestration composes the shipped REST + agent CLI + observer view; distinct seats are
guaranteed by the server's race-safe assignment plus the launch ordering. No seam/architecture change.

## Next Steps
- review-and-fix-issues v04.02 → release v04.02.00 → END OF PHASE v04: HARDEN opt-in (no --harden) →
  ask; then the phase report.
