# v01.01 — GitHub Issues

Issues for phase **v01.01 — Project skeleton & the game engine** (version **v01 — Game Core & Server Foundation**),
derived from the per-phase Tasks in [roadmap.md](../roadmap.md) (§v01.01) and the
contracts in [architecture.md](../architecture.md) (§2 module layout, §4.1 `GameInterface`, §11 testing).
This file is scoped to a single phase; IDs use the **`ARENA-OPUS-###`** namespace — this branch's
issues live in the *shared* `agent-arena` GitHub repo alongside sibling branches' `ARENA-###`
issues, so the `-OPUS-` prefix keeps this independent build's IDs globally unique. This is the first
phase for this branch — IDs start at **ARENA-OPUS-001…ARENA-OPUS-003**.

This phase stands up the runnable project skeleton (toolchain + package layout) and builds the
first stable seam — the abstract **`GameInterface`** (§4.1) — with its concrete **TicTacToe**
implementation as pure logic behind that seam. No server, transport, persistence, or LLM is
involved: the engine is verified entirely in isolation. It is the gate every later phase builds on.

## Issues Summary Table

| # | ID | Title | Size | Area | Phase | Dependencies |
|---|----|-------|------|------|-------|--------------|
| 1 | ARENA-OPUS-001 | Repo skeleton, toolchain & package layout | S | repo | v01.01 | -- |
| 2 | ARENA-OPUS-002 | `GameInterface` abstract seam + contract test | S | games | v01.01 | ARENA-OPUS-001 |
| 3 | ARENA-OPUS-003 | TicTacToe engine + exhaustive unit tests | M | games | v01.01 | ARENA-OPUS-002 |

**Size legend:** S = 1–2 days, M = 3–5 days, L = 5–8 days

---

## Dependency Tree

```
ARENA-OPUS-001 (repo skeleton, toolchain & package layout — the gate)
  |
  v
ARENA-OPUS-002 (GameInterface abstract seam + contract test)
  |
  v
ARENA-OPUS-003 (TicTacToe engine + exhaustive unit tests)  => phase v01.01 DoD
```

**Parallelization hints:** This phase is a short, strictly linear chain — there is no parallelism
across issues. ARENA-OPUS-001 is the gate (nothing compiles or tests until the toolchain + layout
exist); ARENA-OPUS-002 pins the seam once the tree is green; ARENA-OPUS-003 implements against the
pinned seam. Within ARENA-OPUS-003, authoring the exhaustive test matrix can proceed alongside the
engine code, but both land in the one commit (tests ship with the feature).

---

## v01.01 — Project skeleton & the game engine

### ARENA-OPUS-001 — Repo skeleton, toolchain & package layout

**Description:**
Stand up the runnable project skeleton so pytest + mypy run green on an (almost) empty tree. Creates
`pyproject.toml` (pytest + strict mypy config) and the full package layout from [architecture.md](../architecture.md) §2
(`games/`, `server/`, `agent/`, `web/`, `profiles/`, `scripts/`, `tests/`). Touches: repo root, all
package `__init__` files, `tests/`. Extends (does not recreate) the existing `.gitignore`.

**What needs to be done:**
- `pyproject.toml`: project metadata; dev deps for the toolchain (`pytest`, `mypy`); `[tool.pytest.ini_options]` (testpaths = `tests`); strict mypy config (`[tool.mypy]` with `strict = true`) — or a `mypy.ini` if preferred, but keep one source of truth.
- Create the package directories per §2: `games/`, `server/`, `agent/`, `web/`, `profiles/`, `scripts/`, `tests/`; add `__init__.py` to the Python packages so imports and mypy resolve.
- Ensure `.gitignore` covers `.venv/`, `__pycache__/`, `*.db`, and local env files (append only if missing — the file already exists).
- A trivial smoke test (`tests/test_smoke.py`) asserting the toolchain runs (e.g. `assert True` / a version import), so `pytest` exits 0 and `mypy` is clean on a fresh checkout.

**Dependencies:** None

**Expected result:**
A fresh checkout can `pytest` (green) and `mypy` (clean) with the full package layout in place — the substrate every later issue imports into.

**Acceptance criteria:**
- [ ] `pyproject.toml` exists with pytest config and **strict** mypy config; `tests/` is the pytest root.
- [ ] The §2 package directories exist with `__init__.py` where needed; `import games`, `import server`, `import agent` resolve.
- [ ] **Unit test:** `tests/test_smoke.py` runs and passes under `pytest` (no LLM, no network, no paid call).
- [ ] `mypy` runs clean (strict) on the skeleton; `.venv/`, `__pycache__/`, `*.db` are gitignored.
- [ ] Ties to DoD: provides the runnable project the fully-tested engine lives in.

---

### ARENA-OPUS-002 — `GameInterface` abstract seam + contract test

**Description:**
Define the abstract **`GameInterface`** — the only way a game plugs into the system ([architecture.md](../architecture.md) §4.1) —
as four typed abstract methods, importing nothing from `server/`. This is a **stable seam**: it ships
with a contract test that pins the exact method signatures, and any future change to it must update
[architecture.md](../architecture.md) §4.1 + this test in the same commit. Touches: `games/interface.py`, `tests/`.

**What needs to be done:**
- `games/interface.py`: `GameInterface(ABC)` with the four `@abstractmethod`s, strictly typed exactly per §4.1:
  - `get_state(self) -> dict[str, Any]`
  - `get_valid_moves(self) -> list[Any]`
  - `apply_move(self, player: str, move: Any) -> bool`
  - `is_game_over(self) -> str | None`
- Document the seam rules in the module: the move payload is **opaque** to transport; `apply_move` is the **sole legality authority** and returns `False` (never raises) on illegal input; new games are new modules.
- Keep the module free of any `server/` import (pure logic seam).

**Dependencies:** ARENA-OPUS-001

**Expected result:**
An importable, strictly-typed `GameInterface` abstraction whose signatures are pinned by a contract test.

**Acceptance criteria:**
- [ ] `games/interface.py` defines `GameInterface(ABC)` with the four abstract methods, strict-typed per §4.1; imports nothing from `server/`.
- [ ] **Contract test:** `tests/test_game_interface_contract.py` pins the seam — the four method **names + signatures** (params and annotations) are asserted (e.g. via `inspect.signature`), and a bare/partial subclass cannot be instantiated (abstractness enforced). *(Seam pinned — architecture §4.1 is its source of truth; changing the seam updates §4.1 + this test together.)*
- [ ] **Unit test:** the abstractness/signature assertions run under `pytest` with **no LLM and no paid call**.
- [ ] `mypy` (strict) is clean on `games/interface.py`.
- [ ] Ties to DoD: establishes the abstract game seam the TicTacToe engine implements.

---

### ARENA-OPUS-003 — TicTacToe engine + exhaustive unit tests

**Description:**
Implement the concrete **TicTacToe** game as pure logic behind `GameInterface` ([architecture.md](../architecture.md) §4.1),
plus the exhaustive unit suite that verifies it in isolation (roadmap §v01.01 Tests). No server,
transport, or persistence is involved. Per "tests ship with the feature," the engine and its full
test matrix land in the one commit. Touches: `games/tictactoe.py`, `tests/`.

**What needs to be done:**
- `games/tictactoe.py`: `TicTacToe(GameInterface)` — a 9-cell board (`list`), `X` moves first, the 8 winning lines, `"draw"` when the board is full with no line.
- `get_state()` returns the structured board (e.g. `{"board": [...]}`); `get_valid_moves()` returns the list of empty cells (shrinks as the board fills); `is_game_over()` returns `"X" | "O" | "draw" | None`.
- `apply_move(player, move)`: the **sole legality authority** — returns `False` for out-of-range, occupied, and wrong-type (non-`int`) moves and **never raises**; on a legal move, marks the cell with `player`'s symbol and returns `True`.
- Exhaustive tests (`tests/test_tictactoe.py`): a **win on every one of the 8 lines**; **draw** detection on a full no-line board; **illegal-move rejection** for each category (out-of-range, occupied, wrong-type) returning `False` without raising; `get_valid_moves` **shrinks correctly** as moves are applied; `get_state`/`is_game_over` report correctly through a full game.

**Dependencies:** ARENA-OPUS-002

**Expected result:**
A fully-tested TicTacToe engine that reports valid moves, applies legal moves, rejects illegal ones without raising, and detects win/loss/draw — all verified with no server involved.

**Acceptance criteria:**
- [ ] `games/tictactoe.py` implements `GameInterface`; `X` first, 8 winning lines, `"draw"` on a full board; `apply_move` returns `False` (never raises) for out-of-range/occupied/wrong-type and `True` on a legal move.
- [ ] **Unit test:** the exhaustive matrix passes — win on **each** of the 8 lines, draw detection, illegal rejection per category, `get_valid_moves` shrink invariant — with the **LLM absent entirely** (pure logic, no paid call).
- [ ] `mypy` (strict) is clean on `games/tictactoe.py` and its tests.
- [ ] Ties to DoD: the engine correctly reports valid moves, applies legal moves, rejects illegal ones without raising, and detects win/loss/draw — verified in isolation, no server involved.

---

## v01.01 scope notes

**Total effort:** ~1 week (2 × S + 1 × M) — a short, linear phase.
**Critical path:** ARENA-OPUS-001 → ARENA-OPUS-002 → ARENA-OPUS-003.
**ID namespace:** `ARENA-OPUS-###` — distinct from sibling branches' `ARENA-###` in the shared repo; future phases on this branch continue from ARENA-OPUS-004.
**Phase DoD (roadmap §v01.01):** the TicTacToe engine correctly reports valid moves, applies legal moves, rejects illegal ones without raising, and detects win/loss/draw — verified in isolation, no server involved.
**Contracts pinned this phase:** `GameInterface` (§4.1) — pinned by the contract test in ARENA-OPUS-002; any change updates [architecture.md](../architecture.md) §4.1 + that test together. (`LLMClient` and the WS wire contract are pinned in later phases.)
**Model note:** no model is touched this phase — the engine is pure logic and there is no `LLMClient` yet. The `LLMClient` seam (Anthropic Haiku behind it) arrives in v02.01 and is **always mocked** in tests; **no paid APIs** are called in any test or CI run, ever.
**Companion documents:**
- [roadmap.md](../roadmap.md) — version goals, per-phase Tasks/DoD/Tests (§v01.01).
- [architecture.md](../architecture.md) — §2 (module & directory layout), §4.1 (`GameInterface`), §11 (testing strategy).
- Generated on upload: `v01.01-github-report.md` (ARENA-OPUS-xxx → GitHub #), then `v01.01-execution-report.md`.
