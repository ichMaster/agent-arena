# Code Review & Fix Recommendations — v04.01 (contrasting personas)

**Date:** 2026-07-21 · **Reviewer:** Claude Code (Opus 4.8), via /ship-phase
**Scope:** the two persona YAMLs (`profiles/aggressive.yml`, `profiles/cautious.yml`) and the roster
load/validate test.
**Method:** manual read against `AgentProfile` + the product intent (§1 theater).

## Recommendation summary

| # | Severity | Finding | Recommendation | Status |
|---|----------|---------|----------------|--------|
| 1 | 🟡 LOW | Persona *quality* (does the banter actually land?) is unasserted — judged only by the live demo | Accept — quality is inherently a demo judgement (roadmap §v04.01 says so); tests cover validity/distinctness | ✅ accepted |

**No 🔴/🟠 findings and no fix-now items.** Both profiles are pure data validated by the shipped
`AgentProfile` (bounded temperature, positive memory, unknown keys rejected); no code contract touched.

## Findings

### #1 🟡 Persona quality unasserted (LOW) — accepted
The tests assert both profiles load, are valid, are distinct, and drive the factory — but not that the
trash-talk is entertaining. That is intentional: the roadmap explicitly leaves persona quality to the
demo, and asserting model output would require a paid call (forbidden in tests). Kept.

## What's solid
Both profiles exercise every `AgentProfile` field; the contrast is real (name, tone, temperature 0.9
vs 0.4, memory 10 vs 6); validity of *play* is guaranteed independently by `choose_move`'s
retry→random-legal fallback; zero model calls in the suite.

## Architecture impact
None — profiles are data for the existing `AgentProfile`/`LLMClient` seams.

## Suggested next actions
Release v04.01.00, then v04.02 (the `run_arena.sh` orchestration script — the v04 release gate).
