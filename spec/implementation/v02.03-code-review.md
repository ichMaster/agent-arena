# Code Review & Fix Recommendations — v02.03 (agent CLI, decision, session)

**Date:** 2026-07-21 · **Reviewer:** Claude Code (Fable 5), via /ship-phase
**Scope:** the v02.03 additions in `agent/agent.py` (CLI, `choose_move`, `AgentSession`, `run_agent`)
and their stiches to the v01 server protocol and the v02.01/.02 building blocks.

## Recommendation summary

| # | Severity | Finding | Recommendation | Status |
|---|----------|---------|----------------|--------|
| 1 | 🟡 LOW | No reconnect/backoff in `run_agent` — a dropped connection ends the process | DEFER → v05.01 (resilience is explicitly its scope) | ⏳ deferred |
| 2 | 🟡 LOW | `choose_move` catches broad `Exception` around the model call | Accepted design (resilience: any model failure = failed attempt, never a stall) — keep | ✅ accepted |
| 3 | 🟡 LOW | `.env` is resolved from the CWD — behavior depends on launch directory | DEFER → v05.03 README (document the run modes) | ⏳ deferred |
| 4 | 🟡 LOW | Seat token rides the WS URL query | Already tracked as v01.04 review **#8** (MVP-accepted, §6.3) — no new entry | — |

**No 🔴/🟠 findings and no fix-now items** — the session core is transport-free and integration-proven
against the real server; the decision path degrades gracefully (retry-with-feedback → legal fallback);
the terminal-update and observer no-act rules are enforced and tested by call counts.

## Cross-doc status update (this cycle)
- **v01.04 review #7 (no `.env` loader) → ✅ FIXED** in ARENA-OPUS-022 (`01fa414`):
  `load_environment` runs `load_dotenv` before the fail-fast `load_api_key`, exactly as that finding's
  "home: v02.03" prescribed. The v01.04 review doc's status flips in the same commit as this file.

## What's solid
The CLI meets the roadmap invocation verbatim; the retry prompt feeds the rejection back (better than
spec — a blind re-roll would waste attempts at low temperature); memory records only opponent
moves/others' chat (bounded + injection-guarded from v02.02); `agent/` still imports nothing from
`server/`; the whole version adds zero model calls to the test suite.

## Architecture impact
None — the agent consumes the §6.2 protocol and §4.2 seam as documented; no contract changed.

## Suggested next actions
Release `v02.03.00`, then the end-of-phase HARDEN sweep (pre-approved via `--harden`) for the
outstanding v01.04 MEDIUMs #2/#4.
