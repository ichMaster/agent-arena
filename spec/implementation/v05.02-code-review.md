# Code Review & Fix Recommendations — v05.02 (end-to-end integration suite)

**Date:** 2026-07-21 · **Reviewer:** Claude Opus 4.8, via `/ship-solution`
**Scope:** `tests/conftest.py`, `tests/test_no_paid_call_guard.py`, `tests/test_agent_vs_agent.py`.
**Method:** manual adversarial read — does a new global autouse patch quietly break an existing test's
own local-mock call-count assertions? Checked directly (the full suite passed with the guard active).

## Recommendation summary
*No findings.* This version is test infrastructure only — no production path changed — and the one
adversarial concern (autouse patch vs. local mocks) was verified, not assumed.

## Findings
None. The autouse guard patches `agent.llm.AsyncAnthropic` at the exact import path existing tests
already patch, so a nested `with patch(...)` in a test still wins for its own block (well-defined
`unittest.mock` behavior) — confirmed by `test_llm_factory.py`'s `assert_called_once_with` still
passing, and by the whole suite (183) staying green.

## What's solid
The agent-vs-agent driver reads one broadcast at a time from both sockets and asserts `e_x == e_o` at
every step — a runtime check that the two connections see an identical broadcast stream, not just an
assumption. It passed deterministically (3/3 repeated runs). Exact LLM call counts (3 for X, 2 for O)
prove neither session acted on the terminal `state_update` — the same discipline the single-agent test
established, now symmetric for two.

## Architecture impact
None — test-only; no seam, event, or contract touched.

## Suggested next actions
Release `v05.02.00` (tag `opus-opus-v05.02.00`, no push). Then v05.03 (polish & docs — the **v05
release gate**, MVP complete).
